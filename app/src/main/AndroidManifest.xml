<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tth14110049.vn.edu.hcmute.smartcook">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Controller.Activity.SplashScreenActivity"
            android:theme="@style/AppTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.Activity.MainActivity" />
        <activity android:name=".Controller.Activity.FoodDetailsActivity" />
        <activity android:name=".Controller.Activity.GetFoodByCategoryActivity" />
        <activity android:name=".Controller.Activity.MenuDetailsActivity" />
        <activity android:name=".Controller.Activity.WeeklyMenuActivity" />
        <activity android:name=".Controller.Activity.AllMenuActivity"></activity>
    </application>

</manifest>